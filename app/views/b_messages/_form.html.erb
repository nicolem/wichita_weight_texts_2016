<%= form_for(@b_message) do |f| %>
  <% if @b_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@b_message.errors.count, "error") %> prohibited this b_message from being saved:</h2>

      <ul>
      <% @b_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
